pluginManagement {
    plugins {
        id 'com.platformlib.gradle-wrapper' version settings['platformlib-gradle-wrapper.version']
    }
}

rootProject.name='platformlib-gradle-wrapper-plugin-ft-single-docker-task'

